{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "npx tsc && mkdir -p ./public && cp -R ../client/build/* ./public && node src/main.js",
    "dev": "npx tsc && node src/main.js",
    "test": "npx tsc && npx jest"
  },
  "author": "Dorian Ferrer",
  "license": "ISC",
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.7",
    "@types/jest": "^26.0.10",
    "dotenv": "^8.2.0",
    "jest": "^26.4.1",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "csv-parse": "^4.12.0",
    "express": "^4.17.1"
  }
}
